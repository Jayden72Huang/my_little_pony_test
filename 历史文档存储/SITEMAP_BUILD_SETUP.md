# Sitemap 自动生成配置说明

## ✅ 已完成的配置

### 1. 自动生成脚本
- **文件位置**：`scripts/generate-sitemap.js`
- **功能**：在每次构建时自动生成 `sitemap.xml` 静态文件
- **输出位置**：`out/sitemap.xml`

### 2. 构建流程集成
- **postbuild 钩子**：在 `npm run build` 后自动运行脚本
- **部署流程**：`npm run deploy` 会先运行 `build`，从而自动生成 sitemap

### 3. 生成内容
- ✅ 15 个 URL（首页、角色页面、指南页面等）
- ✅ 标准 XML 格式
- ✅ 包含最后修改时间（每次构建自动更新）
- ✅ 包含更新频率和优先级

## 📋 工作流程

### 每次构建时：
1. 运行 `npm run build` → Next.js 构建
2. 自动运行 `postbuild` → 生成 `sitemap.xml`
3. 文件保存到 `out/sitemap.xml`

### 每次部署时：
1. 运行 `npm run deploy`
2. 自动运行 `npm run build`（包含 sitemap 生成）
3. 运行 `opennextjs-cloudflare build`
4. 运行 `opennextjs-cloudflare deploy`（上传到 Cloudflare Assets）

## 🔍 验证方法

### 检查 sitemap 是否生成：
```bash
ls -lh out/sitemap.xml
```

### 查看 sitemap 内容：
```bash
cat out/sitemap.xml
```

### 手动运行脚本：
```bash
node scripts/generate-sitemap.js
```

## 📝 注意事项

1. **自动更新**：每次构建时，`lastmod` 时间会自动更新为当前时间
2. **URL 同步**：脚本会自动读取：
   - `app/utils/characterData.ts` 中的角色 slug
   - `app/guides` 目录中的 guide slug
3. **格式验证**：脚本会自动验证生成的 XML 格式是否正确

## 🚀 使用说明

### 正常构建：
```bash
npm run build
# sitemap.xml 会自动生成到 out/ 目录
```

### 部署：
```bash
npm run deploy
# 会自动构建并生成 sitemap，然后部署到 Cloudflare
```

### 仅生成 sitemap（不构建）：
```bash
node scripts/generate-sitemap.js
```

## ✅ 测试结果

- ✅ 脚本运行成功
- ✅ 生成了 15 个 URL
- ✅ XML 格式正确
- ✅ 文件大小：2.6 KB
- ✅ 自动集成到构建流程

## 📊 生成的 URL 列表

1. 首页 (`/`)
2. 角色列表 (`/characters`)
3. 指南列表 (`/guides`)
4. 指南页面 (`/guide`)
5. 隐私政策 (`/privacy`)
6. 服务条款 (`/terms`)
7. 6 个角色详情页面 (`/characters/[slug]`)
8. 3 个指南详情页面 (`/guides/[slug]`)

总共：15 个 URL
